#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

	# Run lint-staged (prettier + eslint). If it fails due to environment/permission
	# issues (EACCES), fall back to running eslint autofix so commits are not blocked.
	npx --no-install lint-staged || npx --no-install eslint --ext .ts src --fix || true


