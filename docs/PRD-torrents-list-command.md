# PRD: Команда просмотра списка добавленных торрентов

## 1. Executive Summary

Пользователям необходима возможность просматривать список всех добавленных торрентов, видеть их статус, просматривать файлы и удалять торренты. В данный момент информация о торренте отображается только в момент скачивания. После завершения скачивания или перезапуска бота пользователь не может получить список своих торрентов.

Требуется реализовать команду `/torrents` (или `/list`), которая отображает все торренты пользователя с интерактивными inline-кнопками для просмотра файлов и удаления — по аналогии с отображением после завершения скачивания.

## 2. Контекст и мотивация

### As-is

- После добавления торрента командой `/dl_<uid>` бот отображает прогресс скачивания.
- После завершения скачивания бот показывает сообщение с командами `/ls_<uid>` (файлы) и `/rm_<uid>` (удалить).
- Нет способа получить список всех торрентов пользователя.
- После перезапуска бота теряется контекст — пользователь не видит свои торренты.

### Проблемы

- Невозможно увидеть список торрентов без знания их uid.
- Невозможно управлять торрентами после перезапуска бота.
- Отсутствует обзор статуса всех скачиваний.
- Пользователь вынужден искать старые сообщения для получения команд.

### Почему важно сейчас

- Улучшение UX для управления торрентами.
- Метаданные уже хранятся в БД (после рефакторинга) — можно легко получить список.
- Логичное расширение функциональности бота.

## 3. Цели и метрики успеха

| Цель                          | Метрика                     | Базовое значение | Целевое значение         |
| ----------------------------- | --------------------------- | ---------------- | ------------------------ |
| Доступность списка торрентов  | Наличие команды `/torrents` | Нет              | Есть                     |
| Управление торрентами         | Inline-кнопки для действий  | Нет              | Файлы, Удалить           |
| Пагинация для больших списков | Поддержка пагинации         | Нет              | Есть (если >5 торрентов) |

## 4. Пользовательские сценарии (User Stories)

### US-1: Просмотр списка торрентов

**Как** пользователь, **я хочу** вызвать команду `/torrents`, **чтобы** увидеть список всех моих торрентов с их статусом.

**Acceptance Criteria:**

- [ ] Команда `/torrents` возвращает список торрентов пользователя.
- [ ] Для каждого торрента отображается: название, прогресс, статус.
- [ ] Если торрентов нет — выводится соответствующее сообщение.
- [ ] Текст локализован на язык пользователя.

### US-2: Просмотр файлов торрента из списка

**Как** пользователь, **я хочу** нажать кнопку "Файлы" у торрента, **чтобы** увидеть список файлов.

**Acceptance Criteria:**

- [ ] Под каждым завершённым торрентом есть inline-кнопка "📁 Файлы".
- [ ] При нажатии — вызывается существующая логика `/ls_<uid>`.
- [ ] Файлы отображаются как отдельное сообщение.

### US-3: Удаление торрента из списка

**Как** пользователь, **я хочу** нажать кнопку "Удалить" у торрента, **чтобы** удалить торрент и файлы.

**Acceptance Criteria:**

- [ ] Под каждым торрентом есть inline-кнопка "🗑 Удалить".
- [ ] При нажатии — вызывается существующая логика `/rm_<uid>`.
- [ ] После удаления — список обновляется (торрент исчезает).

### US-4: Пагинация списка торрентов

**Как** пользователь с большим количеством торрентов, **я хочу** листать список, **чтобы** не получать слишком длинные сообщения.

**Acceptance Criteria:**

- [ ] Если торрентов больше 5 — отображается пагинация.
- [ ] Кнопки "⬅️ Назад" и "➡️ Вперёд" для навигации.
- [ ] Отображается текущая страница (например, "Страница 1/3").

### US-5: Обновление списка торрентов

**Как** пользователь, **я хочу** обновить список, **чтобы** увидеть актуальный статус торрентов.

**Acceptance Criteria:**

- [ ] Кнопка "🔄 Обновить" в списке торрентов.
- [ ] При нажатии — сообщение редактируется с актуальными данными.

## 5. Функциональные требования

### FR-1: Команда /torrents

Система должна предоставить команду `/torrents`, которая отображает список всех торрентов текущего чата.

### FR-2: Формат отображения торрента

Каждый торрент в списке должен отображаться в формате:

```
<b>Название торрента</b>
---
Прогресс: 100%
Статус: Завершён | Скачивается | Раздаётся
Размер: 1.5 GB
[📁 Файлы] [🗑 Удалить]
```

### FR-3: Inline-кнопки

Для каждого торрента должны быть inline-кнопки:

- "📁 Файлы" — только для завершённых торрентов (progress = 100%).
- "🗑 Удалить" — для всех торрентов.

### FR-4: Callback Query обработка

Система должна обрабатывать callback queries:

- `torrents_files:<uid>` — показать файлы торрента.
- `torrents_remove:<uid>` — удалить торрент.
- `torrents_page:<page>` — перейти на страницу.
- `torrents_refresh` — обновить список.

### FR-5: Пагинация

- Максимум 5 торрентов на страницу.
- Навигация через inline-кнопки.
- Отображение номера текущей страницы.

### FR-6: Пустой список

При отсутствии торрентов — вывести локализованное сообщение "У вас нет добавленных торрентов".

### FR-7: Локализация

Все тексты должны быть локализованы через Fluent (`.ftl` файлы).

## 6. Нефункциональные требования

### NFR-1: Производительность

- Получение списка торрентов не должно занимать более 2 секунд.
- Обновление сообщения при callback — не более 1 секунды.

### NFR-2: Надёжность

- Ошибки при получении данных из qBittorrent — корректно обрабатываются с выводом сообщения.
- При удалении несуществующего торрента — корректное сообщение об ошибке.

### NFR-3: UX

- Кнопки должны быть компактными и понятными.
- Эмодзи для визуального различения действий.

## 7. Ограничения и допущения

- Данные о торрентах хранятся в БД (entity TorrentMeta).
- Актуальный статус торрента получается из qBittorrent API.
- Пользователь авторизован (AuthComposer).
- Локаль определяется из настроек чата (ChatSettings).

## 8. Out of Scope

- Массовое удаление торрентов (select all).
- Фильтрация торрентов по статусу.
- Сортировка списка торрентов.
- Изменение приоритета торрента.
- Пауза/возобновление торрента.

## 9. Архитектурный контекст

### 9.1 Существующие компоненты

- `TorrentMeta` — сущность с метаданными торрента (hash, uid, chatId).
- `TorrentMetaRepository` — репозиторий для работы с метаданными.
- `ChatSettingsRepository` — для получения локали чата.
- `QBittorrentClient` — API клиент для qBittorrent.
- `TorrentsComposer` — composer для команд торрентов.

### 9.2 Новые компоненты

- Обработчик команды `/torrents`.
- Обработчики callback queries для inline-кнопок.
- Новые ключи локализации в `.ftl` файлах.

### 9.3 Интеграция

```
User → /torrents → TorrentsComposer
                        ↓
            TorrentMetaRepository.getByChatId()
                        ↓
            QBittorrentClient.getTorrents(hashes)
                        ↓
            formatTorrentsList() + InlineKeyboard
                        ↓
                   User ← Message
```

## 10. UI/UX Wireframe

### Пример сообщения списка торрентов:

```
📋 Ваши торренты (страница 1/2):

━━━━━━━━━━━━━━━━━━━━
<b>Ubuntu 24.04 LTS</b>
Прогресс: 100% ✅
Размер: 4.7 GB
[📁 Файлы] [🗑 Удалить]

━━━━━━━━━━━━━━━━━━━━
<b>Debian 12 Bookworm</b>
Прогресс: 45% ⏳
Скорость: 2.5 MB/s
ETA: 15 мин
[🗑 Удалить]

━━━━━━━━━━━━━━━━━━━━
[⬅️] [🔄 Обновить] [➡️]
```

### Пример пустого списка:

```
📋 Ваши торренты

У вас нет добавленных торрентов.
Используйте поиск для добавления торрентов.
```

## 11. Локализационные ключи

```fluent
# Команда /torrents
torrents-list-title = 📋 Ваши торренты (страница {$page}/{$totalPages}):
torrents-list-empty = У вас нет добавленных торрентов.
torrents-list-empty-hint = Используйте поиск для добавления торрентов.
torrents-list-error = Ошибка при получении списка торрентов.

# Карточка торрента в списке
torrents-item-completed =
    <b>{$title}</b>
    Прогресс: {$progress} ✅
    Размер: {$size}
torrents-item-downloading =
    <b>{$title}</b>
    Прогресс: {$progress} ⏳
    Скорость: {$speed}
    ETA: {$eta}

# Кнопки
torrents-btn-files = 📁 Файлы
torrents-btn-remove = 🗑 Удалить
torrents-btn-refresh = 🔄
torrents-btn-prev = ⬅️
torrents-btn-next = ➡️

# Callback ответы
torrents-removed-success = ✅ Торрент удалён
torrents-removed-error = ❌ Ошибка при удалении торрента
```

## 12. Зависимости

| Зависимость            | Тип      | Статус        |
| ---------------------- | -------- | ------------- |
| TorrentMeta entity     | Internal | ✅ Существует |
| TorrentMetaRepository  | Internal | ✅ Существует |
| QBittorrentClient      | Internal | ✅ Существует |
| ChatSettingsRepository | Internal | ✅ Существует |
| Grammy InlineKeyboard  | External | ✅ Доступен   |

## 13. Риски и митигация

| Риск                                         | Вероятность | Влияние | Митигация                          |
| -------------------------------------------- | ----------- | ------- | ---------------------------------- |
| Большое количество торрентов замедляет ответ | Средняя     | Средняя | Пагинация, лимит 5 на страницу     |
| qBittorrent недоступен                       | Низкая      | Высокая | Показать кэшированные данные из БД |
| Торрент удалён из qBittorrent напрямую       | Низкая      | Средняя | Синхронизация при запросе списка   |

## 14. Acceptance Criteria (Summary)

- [ ] Команда `/torrents` работает и возвращает список.
- [ ] Inline-кнопки "Файлы" и "Удалить" работают.
- [ ] Пагинация работает при >5 торрентах.
- [ ] Кнопка "Обновить" обновляет сообщение.
- [ ] Все тексты локализованы (минимум en, ru).
- [ ] Пустой список отображается корректно.
- [ ] Ошибки обрабатываются и локализованы.
- [ ] Unit-тесты покрывают основные сценарии.
